{
    "$schema": "https://turbo.build/schema.json",
    "globalDependencies": [
        "**/.env.*local",
        "**/.env.*local.example",
        "**/.env.*production",
        "**/.env.*development"
    ],
    "pipeline": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": [".output/**", "dist/**", "dist-app/**"]
        },
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}
